<%= form_with(model: group_contact) do |form| %>
  <% if group_contact.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(group_contact.errors.count, "error") %> prohibited this group_contact from being saved:</h2>

      <ul>
        <% group_contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.label :contact_id, style: "display: block" %>
    <%= form.text_field :contact_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
